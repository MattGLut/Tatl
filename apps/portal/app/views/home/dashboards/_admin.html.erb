<div class="rounded-2xl border border-slate-200 bg-slate-50 p-6">
  <h2 class="text-lg font-semibold text-slate-900">Administration</h2>
  <p class="mt-1 text-sm text-slate-600">Manage the directory and the association record.</p>
  <% if policy(Property.new).create? %>
    <p class="mt-4">
      <%= link_to "Add property", new_property_path, class: "text-sm font-medium text-indigo-600 hover:text-indigo-800" %>
    </p>
  <% end %>
</div>
